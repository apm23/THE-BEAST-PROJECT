# Total clean tool source

Project-authored emergency cleanup source from `DLTB_TOTAL_CLEAN_TOOL.zip`.

Use only when a full local reset is actually wanted. The tool can remove local game/mod/save/cache locations; Steam Cloud server data is not directly deleted by this script.

The binary ZIP itself is local-only. Git stores the source and manifest only.
